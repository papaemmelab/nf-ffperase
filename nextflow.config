params {
    step = "full"
    vcf = null
    bam = null
    reference = null
    model = null
    modelName = null
    outdir = "${projectDir}/results"
    bed = "${projectDir}/assets/gr37.no_mt_unmapped.bed.gz"
    picard = "${projectDir}/assets/picard.jar"
    picardMetrics = null
    minMapq = 0
    minBaseq = 0
    minDepth = 0
    splitReads = 7500000
    splitPileup = 50
    coverage = null
    medianInsert = null
    mutationType = "snvs"
    tsv = "${projectDir}/assets/NO_FILE"
    version = false
    help = false
}

params.outdirPreprocess = "${params.outdir}/preprocess"
params.outdirClassify = "${params.outdir}/classify"

profiles {
    cloud {
        singularity.enabled = false
        docker {
          enabled = true
          runOptions = "--entrypoint ''"
        }
        process.container = "papaemmelab/toil_formalinfixer:v0.0.2"
    }

    hpc_slurm {
        singularity {
            enabled = true
            autoCleanUp = true
            autoMounts = true
            cacheDir = "/data1/papaemme/isabl/opt/singularity"
            envWhitelist = "SINGULARITY_BINDPATH,SINGULARITYENV_LD_LIBRARY_PATH,SINGULARITYENV_LD_PRELOAD"
        }
        process {
            executor = "slurm"
            queue = "componc_cpu,componc_gpu"
            container = "/usersoftware/papaemme/isabl/local/toil_formalinfixer/v0.0.2/toil_formalinfixer_v0.0.2.sif"
        }
    }
    
}